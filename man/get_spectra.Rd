% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectra.R
\name{get_spectra}
\alias{get_spectra}
\title{Get multiple spectra from mzML file}
\usage{
get_spectra(mzml, indices = NULL, ms_level = NULL)
}
\arguments{
\item{mzml}{Object of class \code{\link[=MzMlFile]{MzMlFile()}}}

\item{indices}{Integer vector of spectrum indices to extract (1-based).
If NULL, extracts all spectra.}

\item{ms_level}{Optional integer MS level to filter by.}
}
\value{
A list of lists, where each inner list has the same structure as
the return value of \code{\link[=get_spectrum]{get_spectrum()}}. For large files, consider processing
spectra one at a time to avoid memory issues.
}
\description{
Extracts m/z and intensity data from multiple or all spectra.
}
\examples{
\dontrun{
mzml <- read_mzml("path/to/file.mzML")

# Get first 10 spectra
specs <- get_spectra(mzml, indices = 1:10)

# Get all MS1 spectra
ms1_specs <- get_spectra(mzml, ms_level = 1)
}

}
